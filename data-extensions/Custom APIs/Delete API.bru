meta {
  name: Delete API
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/v2/settings/extensions/custom-apis/{{custom_api_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
